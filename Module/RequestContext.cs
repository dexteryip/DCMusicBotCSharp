using NetCord;
using NetCord.Gateway;
using NetCord.Services;
using NetCord.Services.ApplicationCommands;
using NetCord.Services.Commands;

namespace DCMusicBot.Module
{
    // warapper for CommandContext and ApplicationCommandContext
    public class RequestContext
    {
        //private GatewayClient client;
        //private Guild? guild;
        //private TextChannel? channel;
        //private User user;
        //public RequestContext(GatewayClient client, Guild? guild, TextChannel? channel, User user)
        //{
        //    this.client = client;
        //    this.guild = guild;
        //    this.channel = channel;
        //    this.user = user;
        //    this.guild = guild;
        //}
        //public RequestContext(CommandContext commandContext)
        //{
        //    this.client = commandContext.Client;
        //    this.guild = commandContext.Guild;
        //    this.channel = commandContext.Channel;
        //    this.user = commandContext.User;
        //}
        //public RequestContext(ApplicationCommandContext commandContext)
        //{
        //    this.client = commandContext.Client;
        //    this.guild = commandContext.Guild;
        //    this.channel = commandContext.Channel;
        //    this.user = commandContext.User;
        //}
        //public GatewayClient Client => client;
        //public Guild? Guild => guild;
        //public TextChannel? Channel => channel;
        //public User User => user;
    }
}
